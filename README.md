# dfl

Personal monorepo of small Go libraries I reuse across the companies and places I work.

Packages:

- [`http`](./http): typed handlers over `net/http`, with structured errors and a pluggable backend. See [`examples/`](./examples).
